export default function JobsPage() {
  return (
    <section>
      <h1>Jobs</h1>
      <p>In this starter, the worker runs and waits for jobs. Hook a UI here later (RQ dashboard or custom).</p>
    </section>
  );
}
